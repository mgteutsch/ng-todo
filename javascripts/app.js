"use strict";

var app = angular.module("TodoApp", ["ngRoute"]);

//in order to use angular-route package, we must type in "ngRoute".... 
//go to angular-route.js to find the correct terminology
//if you use a different installer (like npm), the "ngRoute" will be different. You need to look it up   


